# macOS
.DS_Store

# local Claude Code / tooling settings (not part of the site)
.claude/
.gstack/

# project spec — keep local only, not in the public repo
CLAUDE.md

# unrelated local folder containing secrets — never commit
Socials-automation/
